import React from 'react';

const LoaderComponent: React.FC = () => (
    <div className="appContainer">
        <img src="/assets/img/logo.png" alt="Logo" className="appLogoAnimated" />
    </div>
);

export default LoaderComponent;
